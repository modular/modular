load("//bazel:api.bzl", "lit_tests")

lit_tests(
    name = "test",
    srcs = glob(["**/*.test"]),
    data = glob([
        "**/*.txt",
    ]),
    generate_litcfg = True,
    tools = [
        "//Cache/tools/cache-mgr",
    ],
)
