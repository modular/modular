load("//bazel:api.bzl", "lit_tests")

# gazelle:default_visibility //visibility:private

package(default_visibility = ["//visibility:private"])

lit_tests(
    name = "test",
    srcs = glob(["**/*.test"]),
    data = glob([
        "**/*.txt",
    ]),
    tools = [
        "//Cache/tools/cache-mgr",
    ],
)
