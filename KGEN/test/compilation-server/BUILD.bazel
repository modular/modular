load("//bazel:api.bzl", "lit_tests")

lit_tests(
    name = "compilation-server",
    srcs = glob(["**/*.test"]),
    mojo_deps = [
        "@mojo//:stdlib",
    ],
    tools = [
        "//KGEN/tools/compilation-server",
    ],
)
