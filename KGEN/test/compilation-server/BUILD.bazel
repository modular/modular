load("//bazel:api.bzl", "lit_tests")

lit_tests(
    name = "compilation-server",
    srcs = glob(
        ["**/*.test"],
        exclude = [
            "definition.test",  # TODO: Debug and enable
        ],
    ),
    generate_litcfg = True,
    mojo_deps = [
        "//open-source/mojo/stdlib/stdlib",
    ],
    target_compatible_with = select({
        "//:asan": ["@platforms//:incompatible"],
        "//conditions:default": [],
    }),
    tools = [
        "//KGEN/tools/compilation-server",
    ],
)
