# ===----------------------------------------------------------------------=== #
#
# This file is Modular Inc proprietary.
#
# ===----------------------------------------------------------------------=== #

# RUN: %repl <%s 2>&1 | FileCheck %s

# Note: in REPL mode, expressions are delimited by empty lines.

# Initially define an integer value of 10.
let value = 10
# CHECK: ({{.*}}_"$Int"::_Int>) value

# Check that we can redefine this in a followup expression.
let value = True
# CHECK: ({{.*}}_"$Bool"::_Bool>) value

# Check that we can redefine a function that has a return type defined in the
# same expression.
struct Struct:
    fn __init__(inout self):
        pass
fn xoo() -> Struct:
    return Struct()
let abc = xoo()
# CHECK: (pointer<_"$Expression [5]"::_Struct>) abc =

struct Struct:
    fn __init__(inout self):
        pass
fn xoo() -> Struct:
    return Struct()
let abc = xoo()
# CHECK: (pointer<_"$Expression [6]"::_Struct>) abc =
