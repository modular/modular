load("//utils/bazel/build/rules:lit.bzl", "lit_tests")

lit_tests(
    name = "mojo-lsp-server",
    srcs = glob(
        ["**/*.test"],
        exclude = [
            "definition.test",  # TODO: Debug and enable
        ],
    ),
    mojo_deps = [
        "//open-source/mojo/stdlib/stdlib:stdlib",
    ],
    target_compatible_with = select({
        "//:asan": ["@platforms//:incompatible"],
        "//conditions:default": [],
    }),
    tools = [
        "//KGEN/tools/mojo-lsp-server",
    ],
)
