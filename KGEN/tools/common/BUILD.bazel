load("//bazel:api.bzl", "modular_cc_library")

package(default_visibility = ["//visibility:public"])

modular_cc_library(
    name = "common",
    srcs = glob(["*.cpp"]),
    hdrs = glob(["*.h"]),
    deps = [
        "//Support:Driver",
        "//Support:Telemetry",
    ],
)
